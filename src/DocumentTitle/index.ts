import './document-title.less';
import { attachPropertiesToComponent } from 'antd-mobile/es/utils/attach-properties-to-component';
import { DocumentTitle } from './document-title';

export type { DocumentTitleProps } from './document-title';

export default attachPropertiesToComponent(DocumentTitle, {});
